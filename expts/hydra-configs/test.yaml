defaults:

  # Accelerators
  - accelerator: cpu

  # Pre-training/fine-tuning
  - architecture: test
  - tasks: test
  - training: test

  # Benchmarking
  - model: gcn

  # Specializations
  - training/accelerator: ${training}_${accelerator}
  - training/model: ${training}_${model}
