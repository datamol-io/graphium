# @package _global_

datamodule:
    args:
      # Data handling-related
      batch_size_training: 30
      batch_size_inference: 30

predictor:
  optim_kwargs:
    loss_scaling: 1024

trainer:
  trainer:
      precision: 16-true
      accumulate_grad_batches: 2