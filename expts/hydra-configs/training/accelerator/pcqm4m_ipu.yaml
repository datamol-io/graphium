# @package _global_

datamodule:
  args:
    # Data handling-related
    batch_size_inference: 16

predictor:
  metrics_every_n_train_steps: 100
  optim_kwargs:
    loss_scaling: 1024

trainer:
  trainer:
    precision: 16-true
