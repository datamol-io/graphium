# @package _global_

constants:
  name: toy_data_gated_gcn_depth_6
  wandb:
    name: ${constants.name}
    project: neurips2023-expts
    entity: jmohamud
  entity: multitask-gnn
  seed: 42
  max_epochs: 200
  data_dir: ../graphium/expts/data/neurips2023/small-dataset
  raise_train_error: true
  datacache_path: ../datacache/small-dataset/
  norm: "layer_norm"

trainer:
  model_checkpoint:
    dirpath: model_checkpoints/small-dataset/gated_gcn/${now:%Y-%m-%d_%H-%M-%S}/