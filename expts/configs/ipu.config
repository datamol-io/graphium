deviceIterations(16)
replicationFactor(1)
Training.gradientAccumulation(3)    # How many gradient steps to accumulate
enableProfiling("pro_vision")       # The folder where the profile will be stored
Jit.traceModel(True)
enableExecutableCaching("pop_compiler_cache")
