#
@extends: ultralarge_part1.yaml

constants:
  datacache_path: "/localdata/neurips2023-ultralarge/"

datamodule:
  module_type: "MultitaskFromSmilesDataModule"
  # module_type: "FakeDataModule"  # Option to use generated data
  args: # Matches that in the test_multitask_datamodule.py case.
    task_specific_args:   # To be replaced by a new class "DatasetParams"
      pm6_83m_g62:
        df_path: graphium/data/neurips2023/ultralarge-dataset/pm6_processed_*.parquet
        # wget https://storage.googleapis.com/graphium-public/datasets/neurips_2023/Ultralarge-dataset-v2/pm6_processed_1.parquet
        # Repeat the `wget` command for all 20 files ending with `_1.parquet` to `_20.parquet`

      pm6_83m_n7:
        df_path: graphium/data/neurips2023/ultralarge-dataset/pm6_processed_*.parquet
        # wget https://storage.googleapis.com/graphium-public/datasets/neurips_2023/Ultralarge-dataset-v2/pm6_processed_1.parquet
        # Repeat the `wget` command for all 20 files ending with `_1.parquet` to `_20.parquet`

trainer:
  logger:
    save_dir: logs/neurips2023-ultralarge/
