channels:
  - conda-forge

dependencies:
  - python=3.10
  - boost
  - pybind11
  - librdkit-dev # headers for rdkit only available on conda-forge
  - pytorch
  - numpy
  - rdkit

  - pip:
      - setuptools-scm
      - build