channels:
  - conda-forge

dependencies:
  - python=3.10
  - boost
  - pybind11
  - librdkit-dev # headers for rdkit only available on conda-forge
  - pytorch
  - numpy==1.25
  - rdkit

  - pip:
      - setuptools-scm
      - build